package stacksandqueues.inclass;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.fail;

public class InClassTest {

    @Test
    public void doInClassPushTest() {
        // what behavior do we expect when the stack has data?
        // what behavior do we expect when the stack has no data?
        // are there edge cases or push us cases that we should test
        // - what can we see after multiple calls?
        fail();
    }
    @Test
    public void doInClassPopTest() {
        // what behavior do we expect when the stack has data?
        // what behavior do we expect when the stack has no data?
        // are there edge cases or push us cases that we should test
        // - what can we see after multiple calls?
        fail();
    }
}
